const host = 'http://localhost:2727/'

export const environment = {
  resources: `${host}resources/imgs/result/`,
  convert: `${host}api/conversions/convert`,
  search: `${host}api/conversions/search`,
  resume: `${host}api/conversions/resume`,
};
